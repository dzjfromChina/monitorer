<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.monitor.monitorer.mapper.TestMapper">

    <select id="mySelectPage" resultType="com.monitor.monitorer.entity.Test">
        SELECT id,name FROM test WHERE age=#{age}
    </select>


</mapper>
